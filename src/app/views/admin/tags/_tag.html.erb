<tr>
  <td>
    <%= tag.body %>
  </td>
  <td>
    <%= link_to t('defaults.edit'), edit_admin_tag_path(tag), class: 'btn btn-success' %>
    <%= link_to t('defaults.delete'), admin_tag_path(tag), method: :delete, data: { confirm: t('defaults.message.delete_confirm') }, class: 'btn btn-danger' %>
  </td>
</tr>